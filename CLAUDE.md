# jp-review-shorts — 일본 직접촬영 상품 리뷰 숏츠

상위 공통 규칙은 `Desktop/project/CLAUDE.md` 그대로 적용됨. 이 파일은 이
프로젝트만의 활성 규칙만 담는다.

## 프로젝트 정체성

- **니치**: 사용자가 일본 약국/슈퍼마켓/돈키호테 등에서 직접 촬영한 영상 →
  상품 식별 → 리서치 → 실사 b-roll+클로즈업+정보 카드 조립 숏츠. **"한국
  유사제품과 비교"는 초기에 든 예시 하나일 뿐, 고정 포맷이 아니다(2026-08-10,
  사용자 확정 — "그거에 매몰되면 절대 안 돼, 쓰레기같은 명시야, 방식은
  무궁무진하다")** — topic마다 어떤 각도로 풀지는 "상품 선정 기준" 섹션의
  9가지 렌즈를 참고해 세션이 직접 기획한다(한국 비교는 그중 하나일 뿐).
- **health-shorts와 완전 별개 프로젝트.** 니치를 섞으면 유튜브·틱톡 알고리즘이
  불리해진다는 health-shorts/benefit-shorts의 기존 결정과 동일 원칙 —
  코드·데이터·자산 어떤 것도 health-shorts와 실시간 참조하지 않는다.
  `lib/video_assembler.py`·`lib/bgm.py`·`assets_library/fonts/`는
  health-shorts에서 이식한 것(2026-08-09) — 이후 독립적으로 수정하고,
  health-shorts 쪽 변경을 자동으로 따라가지 않는다. TTS는 이식 대상이
  아니라 이 프로젝트 자체 구현(`lib/fish_audio_tts.py`, 아래 "알려진 함정"
  참고).
- **채널 확정: Basketbrief, 독립 채널(2026-08-14)** — health-shorts와 채널을
  공유하면 니치가 섞여 유튜브 알고리즘의 시청자 클러스터링에 불리하다는
  판단으로(위 "완전 별개 프로젝트" 원칙 그대로 채널까지 분리), `worthitshopping`
  재사용 계획은 폐기하고 새 채널로 확정. "Worth It"류 이름은 BuzzFeed 동명
  리뷰쇼와 니치가 겹쳐 재사용하지 않음(vernhaven-blog가 같은 이유로 개명한
  전례 참고).
- **유튜브 업로드 인프라: `lib/youtube_upload.py` + `lib/youtube_auth_setup.py`
  (health-shorts 이식, 채널 하나뿐이라 대폭 단순화, 2026-08-14)**:
  - `data/<topic>/platform_captions.json`의 `platforms[]`에 `"name": "YouTube
    Shorts"` 항목을 "제목: X\n\n설명란:\nY" 형식 캡션으로 추가하면 업로드
    대상이 된다 — **아직 이 항목을 가진 topic이 하나도 없음**, 캡션 작성이
    다음 콘텐츠 작업.
  - 영상 파일은 `output/<topic>/`에서 `*shorts.mp4` 또는 `demo_preview.mp4`를
    자동으로 찾음(topic마다 렌더링 시 파일명이 달랐음 — 아직 관례 안 굳음).
  - `python3 lib/youtube_auth_setup.py`(health-shorts와 달리 `--channel` 불필요,
    채널 하나) → `.env`에 `YOUTUBE_CLIENT_ID`/`_SECRET`/`_REFRESH_TOKEN`.
  - `python3 lib/youtube_upload.py --backlog`로 캡션+영상 준비된 topic 전부를
    하루 한 개씩(`DAILY_UPLOAD_HOUR_KST=20`, 실측 근거 없는 시작값 — 데이터
    쌓이면 조정할 것) 순서대로 예약 업로드. 단일 topic은
    `python3 lib/youtube_upload.py <topic> [private|unlisted|public] [publish_at]`.
- 이슈는 `lib/mission_control_log.py`의 `report_issue()`로 mission-control(`../../mission-control`)에도 보고됨(2026-08-15, `youtube_upload.py` 업로드 실패 시 연동 — `MISSION_CONTROL_INGEST_URL`/`_SECRET` 미설정 시 조용히 스킵).
- **가격 비교 금지(2026-08-09, 사용자 확정)** — 한국 판매가를 실시간으로
  정확히 못 긁어오는데(네이버쇼핑 최저가 API 접근 불가) 부정확한 가격을
  콘텐츠에 넣으면 역효과라는 판단. 리서치 단계에서 참고용으로 가격을 확인해도
  **영상·캡션엔 가격 비교를 넣지 않는다** — 성분·소재·향·커뮤니티 반응·
  형태 차이 등 가격 아닌 비교 포인트로 콘텐츠를 구성할 것.
- 의약품(OTC 약) 카테고리는 "추천/효능 보장" 톤 금지, 정보 소개 수준으로
  제한(한국 약사법 리스크 — 음식/화장품/잡화는 무관). **의약부외品(일본
  규제상 화장품과 의약품 사이 등급, 예: 데오드란트 유효성분 함유 제품)도
  같은 톤 제한 적용** — "효과가 확실해요" 단정 대신 "이런 성분이 들어있어요"
  식으로 정보만 전달.

## 세그먼트 카탈로그(2026-08-09, 사용자 확정 — 폴더 구조는 아래 "raw_footage
폴더 taxonomy" 섹션이 최신·최종 버전이니 그쪽을 기준으로 따를 것)

원본 클립을 topic 하나에서만 쓰고 버리지 않는다 — 특히 "걸어다니는 거리
b-roll"처럼 특정 상품과 무관한 무드 영상은 여러 topic에서 재사용 가능해야
한다.

**세그먼트 밀도(2026-08-11 확정)**: 긴 원본(연속 매대 훑기·워킹샷)을 "눈에
띄는 하이라이트" 몇 개만 뽑고 넘어가지 않는다 — **화면이 시각적으로 바뀌는
지점마다**(매대 구역 전환, 다른 상품으로 이동, 카메라 앵글 변화 등)
세그먼트를 계속 등록한다. 100초짜리 파일에 세그먼트 2~3개만 있으면 너무
성긴 것 — 연속 매대훑기 구간이면 5~10초 간격으로도 계속 나올 수 있다.

**`broll` 세그먼트 길이(2026-08-12 확정)**: 배경용 b-roll(`tags`에 `broll`
포함 — 거리/매장 워킹샷, 정적 롱테이크 등, `product_examine`이 아닌 것)은
실제 영상에서 짧게짧게 끊어 쓰는 용도라 **출력 길이가 3초를 넘으면 안
된다**. 두 경우를 구분(배경은 `CLAUDE_ARCHIVE.md` 참고):
- **워킹 POV, 실외(도보 이동 중 촬영, DJI든 폰이든 무관)**: 실속도 그대로면
  하이퍼랩스 느낌이 안 나므로(2026-08-11 사용자 피드백) `speed: 3.0`을
  걸어서 원본은 4~7초로 등록해도 출력은 1.5~2.5초가 되게 한다
  (`_trim_and_cover_crop_clip(..., speed=3.0)`).
- **워킹 POV, 매장 내부(`store_interior`/`drugstore`/`product_shelf` 등
  실내 태그)**: 3배속을 걸면 통로가 좁고 물건이 화면 가까이 스쳐가서
  "너무 정신없다"(2026-08-13 사용자 피드백) — 실내 워킹 b-roll은
  `speed` 필드를 아예 안 넣는다(기본 1.0, 실속도 그대로).
- **정적 롱테이크(카메라 고정, 걷는 중이 아님)**: 배속을 걸어도 화면
  변화가 없어 의미가 없으므로, 등록 단계에서 `in`/`out` 자체를 3초
  이내로 짧게 잡는다.

**`data/_footage_catalog.json`**에 실제 쓴(혹은 쓸 만하다고 확인한) 세그먼트를
아래 스키마로 기록한다 — 클립 전체가 아니라 "이 구간(초 단위)"까지 잘라서
등록:

```
{
  "id": "설명적_슬러그_01",          // 카테고리를 이름에서 바로 알 수 있게
  "clip": "raw_footage/...",         // 원본 파일 경로(폴더 taxonomy 참고, 등록 후엔 footage_clips/<id>.mp4로 자동 교체됨 — 아래 "촬영본 물리적 추출" 참고)
  "in": 0.0, "out": 20.0,            // 초 단위 구간(등록 후 footage_clips/ 기준 0.0~구간길이로 자동 갱신)
  "tags": ["postcard_display", ...], // 카테고리·안전성 태그
  "footage_role": "hero" | "supporting" | "broll", // 아래 "footage_role 분류" 참고
  "safety_reviewed": true | false,   // 필수 — 얼굴·번호판·신체부위 규칙대로 사람이 직접 확인했는지. 아래 "촬영본 물리적 추출" 참고
  "speed": 3.0,                      // 있을 때만 — 워킹 POV b-roll 배속(기본 1.0)
  "brand": "..." | null,
  "product_line": "..." | null,
  "scent": "..." | null,
  "subtitle_band": {"ymin":.., "ymax":..} // 있을 때만 — "중국어 자막 처리" 참고
  "source": "own" | "xiaohongshu",
  "used_in": []                      // 이 세그먼트를 쓴 topic 이름 계속 추가
}
```

⚠️ **새 세그먼트는 `data/_footage_catalog.json`을 손으로 직접 편집하지 말고
`lib.vet_and_extract_catalog.register_segment()`를 거칠 것(2026-08-15
도입)** — 손편집은 `footage_role`·`safety_reviewed` 누락이나 실제 파일 없이
카탈로그만 앞서가는 상태를 아무것도 막아주지 않는다. `register_segment()`는
필수 필드를 파라미터로 강제하고 등록 즉시 `footage_clips/<id>.mp4`로 물리적
추출까지 마친다. 자세한 건 아래 "촬영본 물리적 추출 파이프라인" 참고.

새 topic 작업 시작 전에 **폴더를 뒤지지 말고 이 카탈로그부터** 태그
(`street`/`safe`/`product_shelf`/`no_people` 등)·브랜드·제품으로 훑어서
재사용 가능한 세그먼트가 있는지 먼저 확인하고, 없을 때만 raw_footage 원본을
새로 훑는다.

### footage_role 분류 및 영상 조립 시 활용(2026-08-12, 사용자 확정)

세그먼트를 실제 영상 제작에 어떻게 쓸지 3갈래로 미리 분류해둔다 — 폴더는
안 나누고(기존 관례 그대로) `footage_role` 필드로만 구분:

- **`hero`**: 손으로 상품을 들고 돌리며 보여주는 클립(`tags`에
  `hand_holding`/`product_examine`/`product_hold`/`examine` 중 하나라도 포함).
  그 상품을 다루는 topic이라면 **반드시 써야 하는 핵심 컷** — 상품 소개
  구간의 메인 비주얼.
- **`supporting`**: 매대를 훑는 컷인데 특정 상품이 브랜드/제품명으로
  식별 가능한 것(`product_shelf`/`product_shelf_browsing` 태그 + `brand`가
  있거나 `product_line`이 "~매대"/"~브라우징"/"~파노라마" 같은 범주 라벨이
  아니라 구체적 상품명인 경우). 그 상품의 나레이션 구간에서 "매대에 이렇게
  진열돼 있었다" 류 중간 컷으로 활용 — hero처럼 필수는 아니고 있으면 쓰는
  보조 컷.
- **`broll`**: 워킹 하이퍼랩스, 거리/야외 풍경, 브랜드 특정 안 되는 매대
  파노라마 등 특정 상품에 안 묶이는 곁다리 영상. 어떤 topic에도 인트로/
  아웃트로/전환용으로 재사용 가능.

**`supporting` 조회는 사전 색인이 아니라 topic 선정 시점에 텍스트 매칭으로
한다**(전체 브랜드를 미리 색인해두는 건 "화면에 스친 모든 상품을 다 다루려
하지 않는다" 원칙과 어긋나는 과잉 작업) — topic으로 상품 2~4개가 정해지면
그때 `data/_footage_catalog.json`에서 `brand`/`product_line` 매칭으로 찾는다.

**Stage 5(`jp_review_spec.json` 작성) 시 조립 틀**: 인트로(broll 1개) →
상품별[hero 필수 1개 + 있으면 supporting 1~2개 + 정보 카드] → 상품 사이
전환(broll) → 아웃트로(broll, 인트로와 다른 클립). broll은 인트로/전환/
아웃트로에 매번 다른 클립을 써서 반복감이 안 나게 한다.

**사람이 Finder로 직접 훑어볼 수 있는 뷰**: `python3 -m
lib.build_footage_browser` 실행하면 `output/_footage_browser/{hero,
supporting,broll}/`에 `<브랜드>__<세그먼트id>.mp4`(`safety_reviewed:false`인
세그먼트는 `UNREVIEWED__` 접두어가 붙어서 Finder에서 바로 구분됨) 이름의
심볼릭 링크가 생성된다(실제 파일 이동·복사 없음, `footage_clips/`를 가리킴,
브랜드 알파벳순 그룹핑). 카탈로그가 바뀔 때마다 다시 실행해서 갱신 — 이
폴더 자체는 소스오브트루스가 아니라 매번 재생성 가능한 뷰.

**긴 원본(특히 매장 투어형 샤오홍슈 클립)을 통짜 하나로 등록하지 않는다** —
"몇 초짜리 구간이 어떤 부류의 콘텐츠인지" 기준으로 잘게 쪼개서 카테고리별로
별도 세그먼트 등록 + 이 구간이 특정 topic 전용인지 여러 topic 공통인지도
같이 판단해서 기록.

## 샤오홍슈(小红书) 콘텐츠 활용 규칙(2026-08-09, 사용자 확정)

- **저작권을 사용자가 직접 확보한 링크만 쓴다** — 세션이 임의로 샤오홍슈
  영상을 긁어다 최종 영상에 섞으면 안 됨(저작권/플랫폼 정책 리스크). 세션이
  할 수 있는 건 WebSearch로 **공개 인덱싱된 게시물 URL을 후보로 찾아 제시**
  하는 것까지 — 실제 로그인 피드 탐색(앱 인터랙션)은 불가능.
- 사용자가 라이선스 확보한 링크를 주면 `python3 -m lib.xiaohongshu_import
  <url> <설명적 파일명 slug> <브랜드> <제품라인> <향/맛> <라이선스 근거 메모>`로
  다운로드 — `raw_footage/xiaohongshu/<slug>.mp4`에 저장되고(폴더 안 나눔, 실제
  분류는 카탈로그가 담당 — 아래 "raw_footage 폴더 taxonomy" 참고),
  `data/_xiaohongshu_licenses.json`(git 추적 — 용량 작고 컴플라이언스 근거라
  gitignore 대상 아님)에 출처 URL·라이선스 근거·다운로드 날짜가 자동 기록된다.
- 다운로드된 클립도 `raw_footage/<own 클립과 동일한 방식으로>` 프레임
  추출·스코어링(`lib/jp_review_footage.py`) 대상이 될 수 있고, `_footage_catalog.json`
  등록 시 `"source": "xiaohongshu"`로 표시해서 own 촬영본과 구분한다.
- **링크를 받으면 항상**: ①다운로드해서 실제로 어떤 브랜드·제품·향/맛인지
  프레임을 직접 봐서 리서치·확인 후 그 정보로 정확히 카탈로그에 기록(브랜드명
  짐작으로 넘겨짚지 말 것, 단일 상품이 아니라 매장 투어형이면
  brand/product_line/scent 계층에 억지로 끼워맞추지 않고 `product_line`에
  내용을 서술 + `brand`/`scent`는 필요 없으면 `null`) ②원본은 자막이 박힌
  채로 그대로 저장한다(전처리로 지우지 않음) — 아래
  "중국어 자막 처리" 참고. ③세그먼트로 쪼갤 때 카테고리 태그만이 아니라
  **이 구간이 특정 topic 전용인지 여러 topic에서 공통으로 쓸 수 있는지도
  같이 판단**한다(2026-08-09, 사용자 확정) — 공통으로 쓸 수 있으면
  `raw_footage/xiaohongshu/`에 그대로 두고, 특정 topic에서만 의미 있는 구간
  (예: 그 topic의 도입부 전용 컷)이면 나중에 `data/<topic>/`으로 옮기는 걸
  고려. 지금까지 받은 샤오홍슈 매장투어 클립은 전부 아직 특정 topic에 묶인 게
  없어서 전부 공통 버킷에 있음.

### 중국어 자막 처리(2026-08-09, 실측 후 최종 확정)

**자막을 지우려 하지 않는다 — 화면에서 자막이 뜨는 자리를 항상 불투명 바로
덮는다.** 원본은 자막이 박힌 채로 그대로 저장해두고, topic 조립(Stage 5) 시점에
세그먼트별로 자막 밴드 좌표를 확인해서 스펙에 박아넣는 방식:

- **인페인팅으로 지우는 시도는 실패로 결론남**(`video-subtitle-remover` 오픈소스로
  STTN·LAMA 두 모드 실측 — 조사 경위는 `CLAUDE_ARCHIVE.md` 참고) — 기본
  전략으로 쓰지 않는다.
- **채택한 방식**: 세그먼트를 카탈로그에 등록할 때, 프레임 몇 장을 훑어서
  자막이 뜨는 자리가 그 클립 안에서 고정돼있는지 확인하고(대체로 고정돼있음
  — 伊東屋 클립은 80초 내내 y 72~89% 구간 고정) 그 좌표를
  `_footage_catalog.json` 세그먼트에 `"subtitle_band": {"ymin":..,"ymax":..,
  "xmin":..(선택, 기본 0),"xmax":..(선택, 기본 1)}`(원본 클립 기준 0~1 정규화
  비율)로 기록한다. jp_review_spec.json 작성 시 그 세그먼트를 쓰는
  visual_segments 항목에도 이 `subtitle_band`를 그대로 복사해 넣으면,
  `proto_jp_review.py`의 `_trim_and_cover_crop_clip()`이 렌더링 시점에 그
  자리를 검은 바(불투명 94%)로 덮는다 — 자막 유무를 프레임 단위로 따질 필요
  없이 그 구간 전체를 항상 가리므로, 자막이 끊김없이 전체에 깔린 영상도 그대로
  쓸 수 있다.
- **밴드 크기·위치가 화면 과반을 차지하거나 영상 중간에 위치·스타일이
  바뀌면(고정 밴드 하나로 못 가림) 세그먼트로 등록하지 않는다** — 이 경우만
  원래 규칙대로 폐기.
- `subtitle_band` 좌표는 **소스 클립과 최종 출력(1080x1920, 9:16)의 종횡비가
  같다는 전제**다 — 지금까지의 샤오홍슈 클립은 전부 720x1280(9:16)이라 비율이
  그대로 대응되지만, 종횡비가 다른 소스는 cover-crop으로 잘려나가는 영역만큼
  좌표가 어긋나니 주의.

## raw_footage 폴더 taxonomy(2026-08-09, 최종 단순화)

**분류는 전부 `data/_footage_catalog.json`이 담당한다 — 폴더는 얕게, source
기준으로만 나눈다.** 브랜드/제품라인/향 단위로 폴더 트리를 깊게 파는 걸
시도했다가(`GATSBY/아이스데오드란트바디페이퍼/샤르도네재스민티/own/...` 식)
찾아 들어가기만 힘들고 실익이 없어서 폐기 — 카탈로그가 태그·브랜드·
subtitle_band까지 다 갖고 있는데 폴더로 또 분류하면 이중 관리다.

```
raw_footage/
  <타임스탬프>.mp4       # 촬영 원본 중 아직 카테고리 서베이 전(미분류 dump)
  _general/              # 서베이 후 "장소·상품 무관 범용 b-roll"로 확인된 것
  own/                   # 서베이 후 "특정 상품" 촬영본으로 확인된 것(설명적 파일명)
  xiaohongshu/            # 샤오홍슈에서 가져온 것 전부(매장투어든 단일상품이든
                          # 구분 없이 파일명으로만 — kracie_hachimijiogan_a_01.mp4,
                          # joshin_hobby_01.mp4, itoya_ginza_01.mp4)
```

- **폴더명·파일명은 항상 로마자/한글만 사용** — 일본어 원문(브랜드명·제품명)은
  경로에 안 쓰고 카탈로그의 `brand`/`product_line` 필드에 원문+한글 병기로
  남긴다(예: 파일은 `kracie_hachimijiogan_a_01.mp4`, 카탈로그엔
  `"brand": "クラシエ(Kracie)"`).
- **긴 원본을 통짜 하나로 등록하지 않는다** — "몇 초짜리 구간이 어떤 부류의
  콘텐츠인지" 기준으로 잘게 잘라 카테고리별 세그먼트로 등록(postcard_display,
  pen_display_case 식) + 이 구간이 특정 topic 전용인지 여러 topic 공통인지도
  판단해서 기록. 원본 그대로 저장해두면 활용 시점에 다시 훑어야 해서 무의미.

`data/_footage_catalog.json`의 각 세그먼트에 `brand`/`product_line`/`scent`/
`source`(`"own"` 또는 `"xiaohongshu"`)/`tags`/(있으면)`subtitle_band` 필드를
채워서, 새 topic 작업 시작 전에 **폴더를 뒤지지 말고 이 카탈로그부터** 브랜드·
제품·태그 기준으로 훑어 재사용 가능한 소스가 있는지 확인한다.

## 파이프라인 (Stage 0~8)

⚠️ **단일 상품 딥다이브 포맷(2026-08-13 확정, 삼양불닭카레_JP_1로 15회+ 반복
검증)** — 상품 여러 개를 훑는 멀티 상품 리뷰(구 `render()`/`make_info_card_png`)는
폐기됐다. 지금 포맷은 씬 4개 고정 구조: **Scene0(1초 무음 커버컷, 사실상
썸네일) → Scene1(인트로 b-roll + 훅 텍스트 팝인) → Scene2(아이템 클로즈업
정왕복, 최대 2회) → Scene3(칠판 상세 후기 + 위쪽엔 실사용 영상)**. 씬 길이는
`narration.srt` 문장 경계로 렌더 시점에 자동 계산되고(`hook_cue_count`/
`item_cue_count`만 스펙에 지정, 기본 각 1문장 — 나머지 전체 문장이 칠판 구간),
Stage 5에서 타임스탬프를 직접 기입하던 구 방식은 없어졌다.

1. 촬영 원본을 `raw_footage/`에 배치(gitignore 대상, 로컬 전용) — 배치 규칙은
   아래 "raw_footage 폴더 taxonomy" 참고(카테고리 서베이 전엔 루트에 타임스탬프
   파일명 그대로, 서베이 후 `_general/`·`own/`로 재배치)
2. `python3 -m lib.jp_review_footage extract <topic>` — 프레임 추출(초당 2장)
   + numpy 기반 Variance-of-Laplacian 블러 스코어 + 연속 프레임 차분으로
   클립당 상위 25장 후보로 압축(`output/<topic>/_footage_frames/`, gitignore 대상)
3. **상품 식별은 코드가 아니라 세션이 직접 `Read`로 후보 프레임을 본다** —
   패키지 일본어 텍스트 판독, 클로즈업/b-roll 구간 확정. Gemini Vision 등
   신규 API 통합은 의도적으로 안 씀(불필요한 이중 검증 단계). 결과를
   `data/<topic>/identified_products.json`에 기록.
4. 세션이 직접 WebSearch로 "상품 선정 기준"의 9가지 렌즈 중 이 topic에 맞는
   각도로 리서치(한국 비교는 그중 하나일 뿐, 매번 쓰는 고정 포맷 아님) →
   `data/<topic>/research_notes.md`(출처 포함, 법적 소명용). ⚠️ **칠판
   불릿은 일반 정보가 아니라 실제 검색되는 비교/수치/제3자 후기여야 한다**
   (예: "카레LEE보다 25배 매움"류 — 아래 "콘텐츠 기획 원칙" 참고).
5. `data/<topic>/jp_review_spec.json` 작성(아래 스키마) — narration.txt도
   같이 작성하되 **직접 화법으로**(하단 "나레이션 톤" 참고).
6. `lib.fish_audio_tts.synthesize(topic, text)` — topic당 TTS 1회 원칙.
7. `python3 -m lib.jp_review_footage assemble <topic>`(또는
   `lib.templates.proto_jp_review.render_single_product()` 직접 호출) —
   TTS(없으면 생성) → 4씬 조립 → BGM 믹스 → 인스타그램 세이프 버전까지 한
   번에. 실사용 영상(`raw_footage/user_trim/<topic>_usage.mp4`)이 아직
   없으면 상품 사진 정지컷 플레이스홀더로 대체하고 `data/<topic>/
   pending_clips.json`에 "채워야 할 자리"를 자동 기록 — `lib/local_studio.py`
   (아래 "로컬 스튜디오 도구" 참고)로 채우면 자동 재조립됨.
8. `platform_captions.json`은 health-shorts 한국어 topic 관례(9플랫폼
   로테이션, `[광고]` 태그, `products` 필드) 그대로 이식해서 쓴다.

### `jp_review_spec.json` 스키마(단일 상품 딥다이브)

```jsonc
{
  "hook_lines": ["첫 줄(흰색)", "둘째 줄(골드, 반전/훅 포인트)"],
  "product_tag": "상품 태그 필/뱃지에 쓰는 짧은 이름",
  "product_photo": {"clip": "...", "at": 1.5},  // 또는 {"image": "data/<topic>/xxx.jpg"}
  "intro_broll": [{"clip": "...", "in": 0.0, "out": 3.0, "speed"?: 1.0, "subtitle_band"?: {...}}, ...],
  "item_clip": {"clip": "...", "in": 0.0, "out": 4.0, "round_trips"?: 1},  // 최대 2
  "chalkboard": {
    "title": "먹어본 후기"(또는 카테고리에 맞는 명사, 아래 참고), "bullets": ["...", ...],
    "usage_steps"?: ["...", ...],  // 실제 사용법·복용법이 있는 topic만(2026-08-13 도입)
    "usage_label"?: "사용법"  // 기본값 "사용법", 식품이면 "먹는 법" 등으로 교체 가능
  },
  "usage_clip_subtitle_band"?: {...},  // 실사용 영상이 샤오홍슈 소스면
  "usage_placeholder"?: {
    "description": "...", "xiaohongshu_query": "...",
    "source_url"?: "...",   // 실제 후기 원문이 있는 페이지(아래 참고)
    "quotes"?: [{"japanese": "원문 그대로", "korean": "번역"}, ...]
  },
  "hook_cue_count"?: 1,  // narration.srt 기준 Scene1이 차지하는 문장 수
  "item_cue_count"?: 1   // Scene2가 차지하는 문장 수(나머지 전부 Scene3)
}
```

- `chalkboard.title`은 상품 카테고리에 맞춰 다르게 — 음식류는 "먹어본 후기",
  화장품/위생용품류는 "사용 후기", 리서치 스크롤로 대체된 topic은 "후기
  모음"(아래 "나레이션·칠판 콘텐츠 품질 원칙" 참고).
- `usage_placeholder`가 있으면 실사용 영상이 없을 때 자동으로
  `pending_clips.json`이 생성/갱신된다 — `description`은 어떤 장면이 필요한지
  간단히, `xiaohongshu_query`는 그 장면을 찾을 때 쓸 중국어 검색어.
- ⚠️ **콘텐츠 퀄리티는 "자료를 많이 찾았는가"가 아니라 "실제 사람이 쓴 원문을
  그대로 썼는가"에서 갈린다(2026-08-13 확정, 킷캣_사쿠라니혼슈 topic에서
  검증)** — `source_url`은 스펙/상품소개 페이지가 아니라 **실제 후기(블로그·
  @cosme·구매평)가 달린 원문 페이지**를 찾을 것. `quotes`엔 그 문장을
  요약·재창작하지 말고 **원문 그대로**(일본어) 뽑아 번역만 붙인다 — AI가
  자기 말로 "이 제품은 향이 강해요"라고 새로 쓰는 것보다, 실제 리뷰어 문장을
  그대로 인용하는 쪽이 훨씬 구체적이고 신빙성 있다("너무 기계적이다/AI
  톤이다" 피드백에 대한 실효적 해결책이었음). 좋은 후기는 항상 만장일치가
  아니다 — 긍정 일색으로 고르지 말고 비판/아쉬움이 섞인 문장도 최소 1개는
  포함할 것(더 진짜처럼 읽힘). 나레이션은 이 인용문을 "누구는 ~라고 했고,
  또 누구는 ~라고 하더라고요" 식으로 직접 참조하며 짜야 한다 — `quotes`를
  화면 인용카드로만 보여주고 나레이션은 따로 노는 구성 금지.

## 로컬 스튜디오 도구(`lib/local_studio.py`, 2026-08-13)

로컬 전용 Flask 앱(인증 없음, `127.0.0.1:5151`만 바인딩 — 절대 `0.0.0.0`으로
안 열 것) — 실행: `.venv/bin/python3 -m lib.local_studio`, 브라우저로
`http://127.0.0.1:5151` 접속.

- **대시보드**: `pending_clips.json`이 있는 topic을 자동으로 나열, 필요한
  설명·샤오홍슈 검색어·필요한 길이(narration.srt 기준 실시간 계산)를 보여준다.
- **트림 툴**: 기존 `raw_footage/` 파일 중 골라서 쓰거나 새로 업로드 → `<video>`
  플레이어로 미리보면서 시작/끝 초를 입력(재생 위치를 시작값으로 바로 채우는
  버튼 포함) → "잘라서 적용" 누르면 ffmpeg로 무음 트림해서
  `raw_footage/user_trim/<topic>_usage.mp4`에 저장하고 **그 자리에서 바로
  `render_single_product()`를 다시 호출**해서 최종 mp4까지 갱신한다. 길이가
  모자라면 렌더 쪽에서 자동 반복재생(정지 프레임 반복 아님)으로 채운다.
- **샤오홍슈 가져오기**: URL+파일명(slug)+라이선스 근거를 입력하면
  `lib.xiaohongshu_import.import_clip()`을 호출 — ⚠️ **license_note는 필수값으로
  강제된다**(샤오홍슈 규칙 "저작권을 사용자가 직접 확보한 링크만" 원칙을 UI
  레벨에서도 지키기 위함, 빈 값이면 요청 자체가 거부됨). 다운로드된 파일은
  트림 툴의 "기존 파일에서 선택"에 바로 나타난다.
- 이 도구는 `hook_lines` 키가 있는(=신규 스키마) topic만 다룬다 — 구 멀티상품
  스키마 topic(예: 갸스비_1, 아래 "알려진 함정" 참고)은 대상에서 자동 제외.

## 촬영본 활용 규칙(2026-08-09, 사용자 확정)

- ⚠️ **카탈로그 태그만 보고 "같은 상품의 다른 구간"이라고 넘겨짚지 말 것
  (2026-08-14 실측, 다국어 확장 중 발견)** — 같은 브랜드/카테고리 태그를
  공유하는 인접 세그먼트라도 실제로는 매대에서 여러 다른 상품·맛·경쟁
  브랜드를 훑는 구간일 수 있다. 실제로 겪은 사고: `curry_roux_box` 태그를
  공유하는 5개 세그먼트 중 2개(`korean_brand`+`spicy` 태그 있음)만 진짜
  삼양 불닭카레였고 나머지 3개는 S&B 골든카레 등 완전히 다른 상품이었음.
  `insect_repellent` 태그 세그먼트 중 하나는 KINCHO의 다른 제품(스프레이가
  아니라 손목링), 또 하나는 아예 다른 브랜드(Fumakilla)였음. `pocky` 태그
  세그먼트도 바로 옆 3초 차이 구간이 다른 맛(유바리멜론)이었음. **특정 topic의
  이미 검증된 세그먼트가 아닌 "비슷해 보이는 다른 세그먼트"를 새로 쓸 때는
  반드시 `ffmpeg -ss <in또는out 근처> -i <clip> -frames:v 1`로 프레임을 뽑아
  직접 눈으로 상품·맛·브랜드가 일치하는지 확인 후 쓸 것** — 태그 매칭만으로는
  불충분하다. 확인할 시간이 없거나 애매하면 원래 topic이 이미 쓰던 세그먼트를
  그대로 재사용하는 쪽이 항상 더 안전하다.
- **얼굴·차량 번호판이 나오는 장면은 활용 금지** — 사람 얼굴이 식별 가능하게
  나오거나 차량 번호판이 읽히는 구간은 b-roll/클로즈업 후보에서 아예 제외.
  Stage 2(numpy 스코어링)는 이 판단을 못 하므로 Stage 3(세션 육안 확인)에서
  반드시 걸러야 한다 — 블러/정지 점수가 높아도 얼굴·번호판이 보이면 탈락.
  - ⚠️ **프레임 한 장만 보고 "safe"로 판단하지 말 것(2026-08-15 실측 사고)** —
    `day2_DJI_20260810180355_0004_D_01`(에비스바시 거리 핸드헬드/드론 컷,
    `far_pedestrians_backs_only`/`safe` 태그로 등록돼 있었음)이 실제로는
    등록된 2.5초 구간 중 앞부분(약 첫 1초)만 안전했고, **구간 후반부(카메라가
    행인들과 가까워지는 지점)에서 얼굴이 또렷이 식별 가능**했다. 신규 topic
    4개가 이 세그먼트를 인트로 브로일로 써서 렌더링까지 됐다가, 최종 mp4
    프레임을 직접 열어본 뒤에야 발견됨(만약 이 리뷰가 없었으면 신원 식별
    가능한 타인 얼굴이 그대로 유튜브에 올라갈 뻔했다). 등록·재사용 양쪽 다:
    구간 **시작·중간·끝 최소 3곳**(핸드헬드/패닝 샷은 더 촘촘히)을 나눠서
    프레임을 확인할 것 — 앞부분만 보고 전체를 판단하지 말 것. 특히
    `raw_footage/ojmo/`(핸드헬드·드론) 소스는 카메라가 계속 움직이므로 이
    위험이 더 크다.
  - ⚠️ **이미 카탈로그에 "safe" 태그로 등록된 세그먼트도 재사용 시 다시
    확인할 것** — 위 사고 사례의 세그먼트는 애초에 "safe"로 등록돼 있었다.
    "카탈로그 태그만 보고 넘겨짚지 말 것"(아래 상품 식별 규칙)과 같은
    원칙을 안전성 판단에도 그대로 적용 — 태그를 신뢰하지 말고, 새 topic에
    쓰기 전에 최소 한 번은 직접 프레임으로 재확인한다.
  - ⚠️ **`safety_reviewed: true`인 세그먼트는 재사용 시마다 확인할 필요
    없다 — `false`인 세그먼트는 topic에 쓰기 전 반드시 사람이 직접 확인
    후 뒤집을 것(2026-08-15 도입, 2026-08-15 전체 812개로 확장)** — 매번
    사람이 다시 확인해야 하는 구조 자체가 반복 사고 원인이라는 판단으로
    시작했고, "확인 안 함"을 카탈로그 필드에 정직하게 남겨서 침묵 속에
    방치되지 않게 했다.
    - **`broll`(190개)**: 전체가 `footage_clips/<id>.mp4`로 물리적으로
      잘려있고(회전 보정·정확한 seek 포함, `raw_footage/` 원본과 무관하게
      독립적으로 존재) OpenCV YuNet(`assets_library/cv_models/
      face_detection_yunet.onnx`)으로 자동 얼굴 검사까지 마쳤다. 얼굴이
      큼직하게(프레임 폭 5% 이상) 잡힌 120개를 사람이 직접 썸네일로
      재확인해서 실제 행인 얼굴 5개만 확정 제외(`excluded`에 기록)했고,
      나머지 전부 `safety_reviewed: true`. **broll을 쓸 때는 더 이상
      `raw_footage/` 원본을 열어볼 필요 없이 카탈로그 값 그대로 써도 된다.**
      플래그된 세그먼트를 한 번에 훑어볼 땐 `python3 -m
      lib.vet_and_extract_catalog sheet`로 `output/_face_review/_sheet_*.jpg`
      그리드 이미지를 생성해서 확인 — id·얼굴 비율이 라벨로 박혀있어
      `_report.json`을 따로 안 봐도 됨.
    - **`hero`/`supporting`(622개)**: `footage_clips/`로 물리적 추출은
      전체 완료(2026-08-15, `lib.vet_and_extract_catalog.run(roles=
      ("hero","supporting"), detect=False)`)됐지만 **자동 얼굴 검사는
      의도적으로 안 걸었다** — 캐릭터 인쇄 포장지·피규어 굿즈가 화면을
      채우는 경우가 많아 자동 얼굴 검사가 오탐(스누피 포장지를 얼굴로
      인식하는 등)을 대량 생성하는 걸 실측 확인했기 때문. 그래서 전체
      `safety_reviewed: false`로 명시적으로 표시돼 있다 — **"파일이
      있다"와 "안전 검증됐다"는 이 카테고리에서 별개다.** 새 topic에
      hero/supporting 세그먼트를 쓰기 전엔 위 "얼굴·차량 번호판" 원칙대로
      구간 시작·중간·끝을 직접 프레임으로 확인하고, 문제없으면
      `lib.vet_and_extract_catalog.mark_reviewed(id)`로 `true`로 뒤집을
      것 — 확인 없이 그냥 쓰지 말 것.
    - **차량 번호판 자동 검사는 role 무관하게 아직 없음** — 얼굴만
      자동화됐고, 번호판은 모든 role에서 여전히 사람이 직접 확인해야 한다.
    - 새로 서베이하는 원본은 `register_segment()`(위 "세그먼트 카탈로그"
      참고)로 등록하거나, broll이면 등록 후 `python3 -m
      lib.vet_and_extract_catalog run`(기본 broll만)을 다시 돌려서 자동
      얼굴 검사까지 받을 것. 카탈로그 정합성(필수 필드 누락, 파일 없이
      카탈로그만 앞서간 상태 등)은 `python3 -m lib.vet_and_extract_catalog
      validate`로 확인 가능 — topic 커밋 전 습관적으로 돌릴 것.
- **손 외의 신체 부위(다리·발 등)가 프레임을 지배하는 구간은 examine
  세그먼트로 등록하지 않는다(2026-08-11, 사용자 확정)** — 매대 사이를 걸어서
  이동하는 전환 컷은 카메라가 자연히 아래(바닥·발)를 향하는데, 이걸 examine
  세그먼트의 시작/끝 경계에 그대로 붙여서 등록한 사례가 실제로 다수 발견됨
  (day2d_20260810_151422 배치에서 최소 6개). 손에 상품을 들고 있는 클로즈업
  프레임 구석에 발 끝이 살짝 걸리는 정도(부수적)는 허용하되, 다리/발이 프레임
  대부분을 차지하거나 제품이 전혀 안 보이는 "걷는 중" 프레임이 `in`/`out`
  경계에 걸리면 그 프레임 이전/이후로 경계를 다시 잡는다. 세그먼트 전체가
  다리/바닥뿐이고 살릴 수 있는 제품 구간이 없으면 세그먼트 자체를 등록하지
  않는다.
- **상품을 들었다 다시 "놓는" 장면은 잘라낸다(2026-08-10 확정)** — 들어서
  살펴보다가 놓기 **직전까지만** 구간을 잘라 등록. **`out` 값은 육안으로
  확인한 마지막 "확실히 들고 있는" 프레임보다 1~2초 더 일찍 잡을 것**(sparse
  샘플링만으론 놓는 동작 시작을 놓치기 쉬움 — 경위는 `CLAUDE_ARCHIVE.md`
  참고) — 놓는 손동작이 한 프레임이라도 걸리는 것보다 살짝 짧게 자르는 쪽이
  항상 낫다.
- **상품이 화면에 너무 많음** — topic 하나를 바로 파고들지 않고, 먼저
  촬영본 전체를 훑어서 어떤 카테고리(약·화장품·과자·잡화 등)가 찍혔는지
  파악하는 "카테고리 서베이" 단계를 Stage 3 앞에 둔다. 그중 **유명한/잘 알려진
  아이템 기준**으로 topic을 골라 그 상품만 집중적으로 데이터를 뽑는다 —
  화면에 스친 모든 상품을 다 다루려 하지 않는다.
- `raw_footage/`에 서베이 전 통째로 던져진 원본(파일명이 촬영 타임스탬프,
  예: `20260809_140613.mp4`)은 카테고리 서베이 후 실제로 쓸 클립만 골라
  `_general/`(범용 b-roll) 또는 `own/`(특정 상품, 설명적 파일명으로 리네임)로
  재배치(원본 삭제하지 않고 이동) — 자세한 폴더 규칙은 "raw_footage 폴더
  taxonomy" 참고.
- ⚠️ **`intro_broll`은 항상 2컷 고정 구조(2026-08-14 확정)** — **1번째
  슬롯 = 거리/야외 영상**(사람 얼굴·차량 번호판 안 걸리는 안전한 클립,
  `tags`에 `street` 포함, day 우선), **2번째 슬롯 = 매대/상품 관련 영상**
  (아이템 리빌 직전이라 "이제 그 매장 안에 들어왔다" 느낌으로 이어져야
  함). 이전엔 "거리 클립은 최후의 수단"이었으나 뒤집힘 — 경위는
  `CLAUDE_ARCHIVE.md` 참고.
  2번째 슬롯(매대) 고를 때는 여전히 이 우선순위로: **①** `brand`가 해당
  topic 브랜드와 일치하는 `footage_role:"supporting"`(넓은 매대/브라우징)
  세그먼트 → **②** `brand`는 달라도 `tags`가 같은 카테고리(예: `foot_care`,
  `drugstore_shelf`, `korean_sign`)인 세그먼트 → **③** 그래도 없으면
  `store_interior`류 일반 매장 내부.
- ⚠️ **한 상품의 실촬영이 짧아서 `item_clip` 정왕복이 부자연스러우면, 먼저
  카탈로그에서 같은 `product_line`(다른 향/색 변형 등)의 인접 세그먼트가
  더 있는지부터 확인할 것(2026-08-13 확정)** — "이거 관련해서 좀더 뭔가
  돌리고 돌리고 하는거보다 더 좋은 방법없을까" 피드백. `item_clip.clips`
  (리스트 형식, `_build_item_scene` 참고)로 실제 여러 컷을 이어붙이는 쪽이
  한 짧은 컷을 앞뒤로 반복하는 것보다 항상 낫다 — 촬영 세션 특성상 같은
  상품의 다른 변형(향/색)을 연달아 찍은 경우가 많아서 후보가 있을 확률이
  꽤 높음.

## 상품 선정 기준(2026-08-10, 사용자 확정 — Stage 3 "유명한/잘 알려진 아이템"의
세부 기준)

카테고리 서베이로 화면에 스친 상품 후보가 추려지면, 그중 실제로 topic화할
상품을 아래 렌즈 중 **하나 이상**에 해당하는지로 고른다. 여러 렌즈에 동시에
해당하면 우선순위 상품(콘텐츠 훅이 여러 개라 스크립트 쓰기도 쉬움):

1. **같은 카테고리 내 향/맛 최고 평가** — 예: 데오드란트 시트 중 이번 시즌
   향이 특히 리뷰가 좋은 것. 소스: @cosme 향 카테고리 랭킹, 편의점 신상 맛
   리뷰 블로그(글리코·롯데 등 시즌 한정 상품 리뷰가 활발함).
2. **일본 커뮤니티 화제작** — 소스: 5ch 검색(`site:5ch.net`), @cosme/LIPS
   랭킹, Yahoo!知恵袋, X(일본어 트윗) 검색, Amazon.co.jp·楽天 리뷰 수·랭킹.
3. **한국 커뮤니티 화제작(한국인 여행객 사이에서)** — 예: "돈키호테 가면
   꼭 사야 하는 것" 류. 소스: 네이버 블로그/카페 "일본여행 꿀템" 검색,
   유튜브 브이로그 댓글·설명란, 인스타그램 해시태그(#일본여행꿀템 등).
4. **비교했을 때 퀄리티가 압도적** — 단순 별점이 아니라 **리뷰 내용의
   구체성**으로 판단(성분·사용감을 구체적으로 설명하는 리뷰가 많으면 신뢰도
   높음, "좋아요"류 리뷰만 많으면 낮음). 블로그의 "OO 3종 비교" 포스팅이
   좋은 소스.
5. **시즌/지역 한정판** — 지금 아니면 못 사는 한정판(예: 이번에 쓴 갸스비
   샤르도네&재스민티 향처럼 국내 미출시 한정 향). "한정"이라는 사실 자체가
   훅이 됨.
6. **한국에 아예 없는 카테고리·기능** — 향만 다른 게 아니라 그 **기능
   자체**가 한국 시장엔 없는 경우(단순 "같은 브랜드, 다른 향"보다 임팩트 큼).
7. **스테디셀러/장수 제품** — 몇십 년째 꾸준히 팔리는 것 — "요즘 뜨는 것"과는
   다른 신뢰도 축의 훅("일본인들이 몇십 년째 쓰는 이유").
8. **재구매율 높다는 언급이 많은 제품** — 리뷰에 "리필만 몇 번째", "다 쓰고
   또 삼" 류 표현이 반복되는지로 판단.
9. **유통 채널 한정(돈키호테 전용 등)** — "이건 돈키호테에서만 판다" 같은
   채널 특이점도 훅이 됨.

**주의**: 위 판단은 리서치 단계(Stage 4)에서 참고용으로만 쓴다 — **가격
비교 금지 규칙**은 그대로 유지(위 "가격 비교 금지" 참고), "가성비"류 렌즈는
쓰지 않는다. 근거 링크는 항상 `research_notes.md`에 남겨서 나중에 법적
소명이 필요할 때 추적 가능해야 한다(추측으로 "커뮤니티에서 화제"라고 쓰지
말 것 — 실제 검색 결과로 확인 후에만 그렇게 서술).

## 나레이션·칠판 콘텐츠 품질 원칙(2026-08-13, 삼양불닭카레_JP_1 반복 피드백 종합)

**영상 퀄리티는 조립 로직이 아니라 이 부분에서 갈린다** — 아래를 매 topic
작성 시 체크리스트로 쓸 것:

- ⚠️ **narration.txt는 예외 없이 존댓말(해요체/합니다체)** — 반말 문장이
  하나라도 섞이면 안 됨(health-shorts와 동일 원칙, 2026-08-13 재확인). 새
  topic 작성 후 제출 전 문장 끝을 전부 훑어서 "-요"/"-습니다"/"-죠" 계열이
  아닌 종결어미(예: "-다", "-어", "-지", "-야")가 있는지 확인할 것 — 단,
  "'아마오우'는 빨갛다·둥글다·크다·맛있다의 앞글자" 같이 **단어 자체를
  인용**하는 경우(사전형 나열, 문장 종결이 아님)는 예외.
- **훅이 던진 전제는 칠판에서 반드시 직접 갚는다** — "한국엔 없다"/"이게
  다르다" 식으로 훅을 잡았으면, 칠판 정보도 그 전제와 직결된 비교·수치여야
  한다. 일반적인 성분·트리비아 나열("5가지 향신료가 들어있어요")은 훅을
  갚지 못해서 "정보가 그저그렇다"는 평가를 받는다.
- **칠판 불릿은 실제로 검색해서 찾은 비교 수치·제3자 후기여야 한다**(가격은
  여전히 금지). 지어낸 뭉뚱그린 주장("반응이 좋아요") 금지 — 예:
  "카레LEE보다 25배 매움"처럼 구체적 비교 대상+수치가 있는 근거를 찾을 것.
  3줄로 담기 부족하면 억지로 압축하지 말고 늘려도 된다 — 분량보다 밀도.
- **나레이션 문장 끝 어미 반복 금지** — 인접한 문장이 같은 종결어미(예:
  "~더라고요"를 연달아 두 번)로 끝나면 "AI가 쓴 것 같다"는 평가를 받는다.
  다 쓰고 나서 마지막 어미들만 다시 훑어 겹치는지 확인할 것.
  **하지만 반말/구어체로 쓰라는 뜻은 아니다** — 존댓말은 유지하되, 사전식
  나열형 문장("먹자마자 순간 얼어붙었다가 땀범벅") 대신 실제 사람이 존댓말로
  구어체로 말하듯("먹자마자 얼음, 땀은 폭포수예요") 짧고 리듬감 있게 쓸 것.
- ⚠️ **문장 "구조" 자체도 반복하지 말 것** — 어미 단어만 바꿔도 모든 문장이
  "[전제절]인데/한데, [본론]-요." 틀 하나로 고정되면 듣기엔 여전히 똑같은
  리듬으로 들린다. 체크리스트: ①모든 문장이 "-는데"로 전제절을 붙이고 있지
  않은지(짧고
  단정적인 문장을 섞을 것 — "원리는 이래요." 처럼 절 없이 끝내는 문장도
  써라) ②문장 길이를 의도적으로 들쭉날쭉하게(한 호흡 짧은 문장 + 두세
  절 엮은 긴 문장을 번갈아) ③칠판 구간처럼 정보 밀도가 높은 문단은 긴
  만연체 하나로 몰아 쓰지 말고 짧은 문장 여러 개로 쪼갤 것(쪼개도 chalkboard
  구간 문장 수는 자유 — hook/item 구간만 narration.srt 문장 수가 spec의
  `hook_cue_count`/`item_cue_count`와 맞아야 하니 그 두 구간만 문장 수 유지).
- **칠판 제목은 "내가 ~" 같은 주어를 안 붙인다** — 어색하다. 카테고리에 맞는
  짧은 명사구로: 실사용 영상(`usage_clip`)이 있으면 "먹어본 후기"/"사용 후기",
  리서치 스크롤(`fulfilled_via_research`, 아래 절 참고)로 대체된 경우는
  "후기 모음"처럼 **개인 경험을 주장하지 않는 표현으로**(2026-08-13 — 실제
  이 컨텐츠는 인용구 번역 모음이지 나레이터가 직접 써본 게 아니라서, "먹어본
  후기"라고 하면 근거 없이 개인 경험을 주장하는 셈이 됨).
- ⚠️ **리서치 스크롤(`fulfilled_via_research`) topic의 나레이션은 "내가 직접
  찾아봤다/뒤졌다/발견했다"류 개인 조사 서사를 안 쓴다** — 대신 "오사카
  돈키호테에 가면 볼 수 있는 ~인데"처럼 **위치·대상을 담백하게 언급하는
  정도**로만 쓰고, 화면(스크롤+인용구 카드)이 "실제로 찾아본 근거"라는 걸
  스스로 보여주게 둘 것 — 나레이션이 그 조사 과정을 서술로 또 설명할
  필요 없음.
- ⚠️ **`hook_lines`(화면에 뜨는 훅 문구)에는 "일본" 또는 "돈키호테"(실제
  구매처가 다르면 그 이름)가 반드시 들어간다** — 훅 문구만 보고도 "일본에서
  구할 수 있는 것"이라는 맥락이 바로 잡혀야 한다. narration.txt 쪽에 이미
  관련 문장이 있어도 화면 훅칩 자체엔 별도로 넣을 것 — `hook_lines`는
  `narration.txt`와 별개로 순수 화면 텍스트라 TTS 재호출 없이 spec.json만
  고쳐서 바로 재렌더링하면 된다.
- **칠판 씬 마지막 구간엔 "블러 배경 + 중앙 Summary 카드" 전환이 자동으로
  들어간다(2026-08-13 도입, `_build_summary_frame`/`_build_chalkboard_scene`
  in `proto_jp_review.py`)** — narration.txt의 **마지막 문장**을 그대로
  가져다 쓰므로(새 TTS 호출 없음), 각 topic의 나레이션 마지막 문장은 항상
  "정리하면 ~" 식으로 실제 한 줄 요약이 되게 써야 자연스럽다(디테일 나열로
  끝내지 말 것).
- **narration.srt 기준 자막이 영상 전체에 자동으로 들어간다(2026-08-13
  도입, `_overlay_narration_captions` in `proto_jp_review.py`)** — 새로
  뭔가 켤 필요 없음, `render_single_product()` 마지막 단계에서 항상 굽는다.
  인트로/아이템 씬은 훅칩 위 안전영역(`_CAPTION_UPPER_Y`)에 `_make_caption_png`
  (가변 크기 박스)로, 칠판 씬은 스크롤+인용카드와 칠판 판 사이에 확보해둔
  전용 밴드(`_CAPTION_BAND_H=140`, `_build_chalkboard_scene`가 칠판 판 비율을
  0.34→0.30으로 줄여서 자리를 만듦)에 `_make_band_caption_png`(높이 고정,
  `_layout_multiline`로 자동 축소)로 굽는다. ⚠️ **밴드 자막은 절대
  `_make_caption_png`를 쓰면 안 된다** — 높이 제약이 없어서 마침표 없이 긴
  단문(특히 인용-attribution 문장, "실제 써본 사람 후기를 보면 '~'는 반응이
  있고요" 류)이 밴드를 벗어나 위/아래 다른 요소를 가리는 사고가 실측
  확인됐음(2026-08-13).
- **`item_clip.clips`(다중 실촬영) 경로는 속도 조정을 절대 안 한다** —
  `_fit_clip_to_duration`(자르거나 반복 재생만, 실사용 클립과 동일 원칙)을
  쓴다. 실촬영 분량이 많아서 목표 길이 안에 다 못 보여줄 때는, 속도로 욱여
  넣지 말고 `item_cue_count`를 늘려서(나레이션 문장 1개→2개) 아이템 씬
  자체에 더 긴 시간을 배정하는 쪽을 먼저 고려할 것 — 시간은 칠판 구간에서
  빌려오게 되는데(칠판은 보통 여유가 많아 부담 적음), 훅 문장을 줄여
  인트로 자체를 짧게 만드는 방법도 있지만 그건 나레이션 재작성+TTS
  재생성이 필요해 비용이 더 큼.
- **`assets_library/music/`에 트랙을 채워야 BGM이 들어간다** —
  `lib/bgm.py`의 `mix_bgm()`은 이미 항상 호출되고 있고 볼륨도 -24dB(내레이션
  절대 안 묻히게, health-shorts와 동일 기준)로 이미 튜닝돼 있음. 트랙이
  하나도 없으면 조용히 BGM 없이 폴백만 하니, 안 들린다고 코드를 의심하기
  전에 먼저 이 폴더 파일 유무부터 확인할 것. 2026-08-13에 health-shorts의
  `assets_library/music/`(유튜브 오디오 보관장 소스, 저작권 걱정 없음)를
  통째로 복사해서 채움 — 같은 소스라 재사용에 문제없음.

## 알려진 함정(구현 중 실제로 겪음)

- ⚠️ **ffprobe csv 출력은 `-show_entries`에 나열한 순서와 무관하게 항상
  고정 순서로 나온다**(실측: `channels,sample_rate`로 요청해도
  `sample_rate,channels` 순으로 출력) — 위치 기반 언패킹(`a, b =
  out.split(",")`)을 쓰면 값이 뒤바뀔 수 있다. 이 버그로 2026-08-13에
  오디오 샘플레이트가 뒤바뀌어 렌더된 5개 topic 전부 QuickTime이 재생
  거부하는 사고가 남(ffmpeg 자체 재생은 통과해서 한참 헤맴 — 조사 과정은
  `CLAUDE_ARCHIVE.md` 참고). **ffprobe csv로 값 두 개 이상 뽑을 땐 항상
  `-of default=noprint_wrappers=1`(key=value 줄) + 딕셔너리 파싱을 쓸 것,
  위치 기반 언패킹 금지.**
- ⚠️ **위 사고 조사 중 별도로 발견한 것: `-pix_fmt yuv420p`만 지정해도 PIL로
  저장한 JPG/PNG를 `-loop 1`로 물리는 단계를 거치면 최종 출력이
  `yuvj420p`(풀레인지)로 태깅될 수 있다** — 이것도 QuickTime 계열 플레이어와
  호환성 문제를 일으킬 수 있어 예방적으로 고쳐뒀다: **모든 `-pix_fmt
  yuv420p` 뒤에 `-color_range tv`를 반드시 같이 붙일 것**
  (`_trim_and_cover_crop_clip()`부터 최종 mux까지 `proto_jp_review.py`
  전체 16곳에 이미 반영됨). 확인 방법: `ffprobe -select_streams v:0
  -show_entries stream=pix_fmt,color_range -of default=noprint_wrappers=1`
  결과가 `pix_fmt=yuv420p`/`color_range=tv`여야 정상(이 항목도 csv 대신
  key=value로 확인할 것 — 위 사고 참고).
- **`data/갸스비_1/jp_review_spec.json`은 2026-08-13 이전 구 멀티상품 스키마
  (products 배열, `visual_segments`)로 만들어진 레거시 산출물** — 이미
  `output/갸스비_1/갸스비_1_shorts.mp4`까지 렌더 완료된 상태고, spec이
  가리키는 `raw_footage/GATSBY/...` 경로도 이후 폴더 taxonomy 단순화로
  사라져서 지금은 재렌더링 자체가 안 된다. `lib/local_studio.py`는 `hook_lines`
  키 유무로 신규 스키마만 걸러서 이 topic을 자동 제외한다. 이 topic을
  신규 파이프라인으로 다시 만들지는 별도 판단 필요(소재 자체는 재사용 가치
  있음 — "브랜드는 한국에도 있지만 이 향/제품만 일본 한정"이라는 훅).

- ⚠️ **Fish Audio 스트리밍 TTS 청크 중복/누락(2026-08-13 확정) — 같은
  `chunk_seq`의 audio는 항상 "처음 온 이벤트만" 쓴다**(`_call_tts_once()`,
  alignment는 계속 최신값으로 덮어씀은 그대로 유지) **+ `_call_tts_batched()`가
  문장 1개당 API를 한 번씩 호출**(`_MAX_SENTENCES_PER_CALL = 1`, 한 호출이
  여러 진짜 청크를 필요로 하면 마지막 청크가 거의 빈 채로 오는 별도 버그가
  있어서). 둘 다 같이 적용해야 안정적 — 조사 과정은 `CLAUDE_ARCHIVE.md`
  참고. Fish Audio 응답이 더 안정화되면 `_MAX_SENTENCES_PER_CALL`을 올리는
  최적화를 시도해볼 수 있음(지금은 안전 우선).
- ⚠️ **나레이션(TTS로 실제 발음되는 텍스트)에는 소수점 퍼센트를 절대
  숫자+기호로 쓰지 말고 항상 말로 풀어쓸 것**(예: "0.09%" 금지 → 영어
  "zero point zero nine percent", 중국어 "百分之零點零九", 한국어 "영점영구
  퍼센트") — 소수점+퍼센트가 섞이면 Fish Audio alignment가 `%`·소수점이
  빠진 토큰만 찍어서 claimed/actual 길이 검증이 결정적으로 실패한다
  (2026-08-14 확인, 정수 퍼센트 "1%"는 문제없음). 칠판 bullet처럼 화면
  텍스트로만 보여주고 TTS가 안 읽는 곳은 숫자+기호 표기 그대로 둬도 무방.
- ⚠️ **`_build_srt()`는 word segment를 재조립하지 않고 `_group_sentences()`가
  반환하는 원문 문장 텍스트를 그대로 SRT에 쓴다**(타이밍만 word segment
  기준) — 예전엔 `" ".join(w["text"] for w in entry)`로 재조립했는데, Fish
  Audio가 중국어는 글자 단위 세그먼트를 반환해서 "也 有 人 說 這..."처럼
  글자마다 공백이 끼었고, 모든 언어에서 word segment에 문장부호가 없어
  콤마·마침표가 자막에서 소실되는 문제도 같이 있었다(2026-08-14 확인).
  **기존에 렌더된 한국어 5개 topic도 이 punctuation 손실을 똑같이 겪고
  있었음** — 재렌더링 여부는 별도 판단 필요, 소급 적용 안 됨.

- **정지 PNG/이미지 루프 입력엔 반드시 `-loop 1` + `-t <길이>` 명시** — `-loop 1`
  없으면 image2 디먼서가 1프레임 스트림으로 취급해 늦게 오는 오버레이가
  안 나오고, `-t` 없으면 무한 스트림이 돼서 ffmpeg이 안 끝난다(둘 다 실측).
- `motion_schedule`/`_build_character_schedule()`은 캐릭터 일러스트 전용
  (크로마키 강제 적용)이라 이 프로젝트에서 쓰지 않는다 — 자연광 실사
  b-roll은 상품 색상이 배경색과 우연히 겹쳐 구멍 뚫릴 위험. b-roll은
  `_trim_and_cover_crop_clip()`(크로마키 없음)로 처리.
- **`raw_footage/폴더 1/` 폰 촬영본은 회전 메타데이터가 신뢰 불가**(2026-08-11
  실측 — `+90` 태그를 ffmpeg autorotate가 잘못 처리해 180도 뒤집힘) —
  `_trim_and_cover_crop_clip()`이 이 폴더 소스에 한해 `-noautorotate` + 수동
  `transpose=1`을 항상 적용. `raw_footage/ojmo/`(DJI)는 회전 태그 자체가
  없어서 해당 없음.
- **`raw_footage/ojmo/`(DJI HEVC)는 fast seek(`-i` 앞 `-ss`만)로 자르면 요청
  시각과 다른 프레임이 나올 수 있다**(키프레임 간격이 넓어 가까운
  키프레임으로 랜딩 — 조사 경위는 `CLAUDE_ARCHIVE.md` 참고). `-i` 뒤 두 번째
  `-ss`(accurate seek)는 `speed`(setpts 배속)와 같이 쓰면 배속이 무시되는
  별도 버그가 있어 안 씀 — 대신 `-i` 앞 coarse seek(목표보다 2초 일찍) +
  `trim` 필터(`trim=start=..:duration=..,setpts=PTS-STARTPTS`)로 seek
  정확도와 배속을 동시에 만족시킴. **이 수정 이전 DJI 세그먼트 프리뷰는
  재검증 없이 신뢰하지 말 것.**

## 세션 안전장치

health-shorts와 동일 원칙(git worktree 격리, 대용량 미디어 gitignore) —
아직 다른 세션과 동시 작업 이력이 없어 `session_lock.py` 이식은 실사용
필요해지면 그때 추가.
